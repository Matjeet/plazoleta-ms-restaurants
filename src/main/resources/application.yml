server:
  port: 8082

spring:
  datasource:
    url: ${db_url}
    username: ${db_username}
    password: ${db_pass}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Pragma Powerup"
appVersion: "1.0.0"
